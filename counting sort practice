unsort = [2,1,6,8,5,6,2]
listlength = len(unsort)

maxval = 8+1

sort = [0]*listlength
index = [0]*maxval
print index

#editing indexes
for i in unsort:
    index[i] += 1
print index

c = 0
for i in range(maxval):
    for a in range(index[i]):
        sort[c] = i
        c += 1
print sort